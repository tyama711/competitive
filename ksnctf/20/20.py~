import sympy
from goto import goto, label

with open('pi.txt', 'r') as f:
    digits = ''
    answer = None
    for line in f:
        digits += line.translate(str.maketrans('', '', '. \n'))
        i = 0
        while len(digits) - i >= 10:
            if digits[i] != '0' and sympy.isPrime(int(digits[i:i+10])):
                answer = digits[i:i+10]
                goto .END
        digits = digits[-9:]
    label .END

print(answer)
